export type Colorable = {
    color?: string;
};

export type CurveDefaults = {
    color?: string;
    width: string;
    height: string;
    viewBox: string;
};

export type SemanticallyHidden = {
    isSemanticallyHidden?: boolean;
};

export type ColorDefault = {
    noDefaultColor?: boolean;
};

export type CurveDefaultsConfig = {
    color?: string;
    width: number;
    height: number;
    viewBox?: string;
};
